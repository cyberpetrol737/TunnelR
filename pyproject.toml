[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "tunnelr"
version = "0.1.0"
description = "Client-side localhost tunneling tool"
requires-python = ">=3.9"

[project.scripts]
tunnelr = "src.tunnelr:main"
